name: Build and Package DSQL

on:
  workflow_dispatch: # Allow manual triggering
  push:
    branches: ["*"] # Run on all branch pushes
  pull_request:
    branches: ["*"] # Run on all pull requests

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, ubuntu-22.04]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v3
        with:
          submodules: "recursive"

      - name: Set up macOS
        if: runner.os == 'macOS'
        run: |
          brew update
          brew install openssl@3 icu4c zip readline

      - name: Set up Ubuntu
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            flex \
            bison \
            libreadline-dev \
            zlib1g-dev \
            libssl-dev \
            libicu-dev \
            build-essential \
            cmake \
            zip \
            rpm \
            patchelf

      - name: Build DSQL
        run: |
          chmod +x scripts/build-dsql.sh
          ./scripts/build-dsql.sh

      - name: Package for Distribution
        run: |
          chmod +x scripts/package.sh
          ./scripts/package.sh

      - name: Test Package
        run: |
          chmod +x scripts/test-packaging.sh
          ./scripts/test-packaging.sh --test-only

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: postgres-dsql-${{ runner.os }}-${{ runner.arch }}
          path: postgres-dsql.zip

      - name: Upload RPM Artifact
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: postgres-dsql-rpm-${{ runner.arch }}
          path: "*.rpm"

      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            postgres-dsql.zip
            *.rpm
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
